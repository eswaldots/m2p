typedef enum HEADING_FONT_SIZE {
        HEADING_H1_SIZE = 20,
        HEADING_H2_SIZE = 18,
        HEADING_H3_SIZE = 16,
        HEADING_H4_SIZE = 14,
        HEADING_H5_SIZE = 12,
        HEADING_H6_SIZE = 11,
        P_SIZE = 12,
} HEADING_FONT_SIZE;

typedef enum mp_log_level_t {
        MP_LOG_ERROR,
        MP_LOG_WARNING,
        MP_LOG_DEBUG,
        MP_LOG_INFO,
} mp_log_level;
